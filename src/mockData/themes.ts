import {colors} from "../assets/colors/colors";

export const themes = {
  lightMode: {
    background: '#fff',
    color: colors.dark,
    borderColor: colors.lightGrey,
    asideColor: colors.darkWhite,
  },
  darkMode: {
    background: colors.lightDark,
    color: '#fff',
    borderColor: colors.grey,
    asideColor: '#1C2733'
  }
}

